The HUD fix didn’t work in practice, even though your logs said it was successful. I need you to investigate what’s actually happening from the user’s perspective and report back without making any changes yet. Current state: I logged out and logged back in as viewonly@aipm.local. The HUD loads but is completely empty — no projects at all. The Proposal Log still shows Welbe Health Center – Test (project 26-1257) correctly for this same user. So the Proposal Log can see the project for ViewOnly, but the HUD cannot. This means the HUD’s filtering logic is doing something different than what your earlier fix addressed. I need you to figure out exactly what. Investigation tasks (do NOT change anything yet): 1. Trace the exact API call the HUD makes when ViewOnly loads the page. What endpoint does it hit? What parameters does it send? What does it actually return for this user? Show me the real request and response, not a theoretical one. 2. Compare that to the API call the Proposal Log makes for the same user. What’s different between the two requests? What’s different between the two responses? 3. Look at the actual database record for project 26-1257 right now, this moment. What is the current value of nbs_estimator? What is the current value of estimateStatus? Has anything changed since your last fix? If the values aren’t VI and Estimating, that means your re-pin on server start isn’t holding — explain why. 4. Look at the actual database record for the viewonly@aipm.local user right now. What is the current value of initials? What is the value of any role or isAdmin field? Confirm these are still what you set them to. 5. Walk through the HUD’s filter logic step by step, using the real current values from steps 3 and 4. At which exact step does Welbe get filtered out for ViewOnly? Is it the initials check, the status check, a role check, or something else I haven’t thought of? 6. Is the HUD doing any frontend-side filtering on top of the backend filtering? In other words, even if the backend returns the project, could the frontend be hiding it? Check both layers. 7. Your earlier fix mentioned “re-pinning on every server start.” Has the server actually restarted since that fix was deployed? If not, the re-pin code may have never run. Verify this. 8. Are there any other places in the code that could be modifying nbs_estimator or estimateStatus on project 26-1257 after the re-pin runs? For example, an auto-repair process, a scheduled job, or a save handler somewhere. List them. Report findings: For each numbered item above, give me a plain-English answer with the actual current values from the live system. Then tell me your best diagnosis of why the HUD is empty for ViewOnly despite your earlier fix appearing to succeed in logs. After your report, propose 2–3 ways to actually fix this — and this time, I want you to verify the fix from the user’s perspective, not just from the logs. Do NOT make code or database changes during this investigation. Just diagnose and report.