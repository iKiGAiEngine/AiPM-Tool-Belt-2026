Stop and don’t make any changes. I just discovered two important things you missed in your last diagnosis. First, the ViewOnly user’s initials are “VO” in development but “VI” in production — dev and prod are out of sync on a key field. Second, I assigned VO to other projects in the development environment, and none of them appeared in the ViewOnly user’s HUD. This means the root cause cannot be “the Welbe project was overwritten at 5:05 AM” — because other projects correctly assigned to VO are also not showing up. The HUD filter logic itself has a problem we haven’t found. Please do the following investigation without making any changes: 1. In your last report you said the HUD uses “userInitials with fallback.” What is the fallback? Walk me through the complete HUD filter logic line by line — every check that runs, in order, to decide if a project shows up for a given user. Use plain English, but reference the actual code file and lines. 2. Test this hypothetically: imagine a project in dev with nbsEstimator = “VO” and estimateStatus = “Estimating”. Imagine the ViewOnly user logged in. Walk through exactly what each step of the HUD filter does with that project. Where does it pass? Where does it fail? Don’t skip any step. 3. Is there a secondary check beyond initials matching? For example, does the HUD also require the user to be in an “active estimator” list, have a specific role, have a non-null department, or any other filter? If yes, does ViewOnly satisfy that secondary check? 4. Compare the entire HUD filter logic to the entire Proposal Log filter logic side by side. The Proposal Log shows projects to ViewOnly correctly. The HUD does not. Identify every difference between the two filters. The difference is the answer. 5. Confirm whether dev and production are actually using the same code version. Has every recent fix you made been deployed to production, or are some changes only in dev? List anything that exists in dev but not yet in production. 6. The ViewOnly initials are “VO” in dev and “VI” in production. Which one is correct? How did this divergence happen? Should I make them match before doing anything else? Report findings in plain English and give me your real diagnosis this time. Don’t tell me the fix is “just assign the estimator manually” — I already tested that approach in dev with other projects and it didn’t work. The HUD has a real bug we haven’t found yet. Do NOT make code or database changes during this investigation.