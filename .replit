modules = ["nodejs-20", "python-3.11", "web", "postgresql-16"]
run = "npm run dev"
hidden = [".config", ".git", "generated-icon.png", "node_modules", "dist"]

[nix]
channel = "stable-24_05"
# cairo/pango/libjpeg/giflib/librsvg/pixman/pkg-config: runtime shared libraries
# the `canvas` npm package (node-canvas) needs to render PDF pages to images
# for Plan Parser thumbnails and OCR. Without these, createCanvas() throws at
# runtime even though the module itself loads fine — which silently dropped
# every processed page before pdfProcessor.ts was made resilient to it.
packages = ["qpdf", "tesseract", "libuuid", "poppler_utils", "cairo", "pango", "libjpeg", "giflib", "librsvg", "pixman", "pkg-config"]

[[ports]]
localPort = 5000
externalPort = 80

[[ports]]
localPort = 23636
externalPort = 3000

[env]
PORT = "5000"

[deployment]
deploymentTarget = "autoscale"
run = ["node", "./dist/index.cjs"]
build = ["npm", "run", "build"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "npm run dev"
waitForPort = 5000

[userenv]

[userenv.shared]
SPEC_EXTRACTOR_URL = "https://5d40cd1d-8149-45b2-bcbd-1f1606bffdab-00-3ftzms78i7wgr.picard.replit.dev"

[postMerge]
path = "scripts/post-merge.sh"
timeoutMs = 20000

[agent]
integrations = ["google-sheet:1.0.0"]
